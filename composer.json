{
    "name": "clinica/taller-seguridad",
    "description": "Taller de Seguridad en Aplicaciones Web — PHP + MySQL",
    "type": "project",
    "require": {
        "php": "^8.2"
    },
    "require-dev": {
        "laravel/pint": "^1.29",
        "phpunit/phpunit": "^11.5 || ^12.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "serve": "php -S localhost:8080 router.php",
        "test": "vendor/bin/phpunit --testdox",
        "test:sql": "vendor/bin/phpunit --testdox --filter SqlInjection",
        "test:validator": "vendor/bin/phpunit --testdox --filter Validator",
        "test:coverage": "XDEBUG_MODE=coverage vendor/bin/phpunit --coverage-text",
        "db:init": "mysql -u root -p < database.sql",
        "pint": "vendor/bin/pint"
    },
    "scripts-descriptions": {
        "serve": "Levanta el servidor PHP en http://localhost:8080",
        "test": "Corre todos los tests con salida descriptiva",
        "test:sql": "Corre solo los tests de SQL Injection",
        "test:validator": "Corre solo los tests del Validator",
        "test:coverage": "Reporte de cobertura de código (requiere Xdebug)",
        "db:init": "Crea la base de datos y la tabla en MySQL",
        "pint": "Formatea el código de todo el proyecto según PSR-12"
    },
    "config": {
        "sort-packages": true,
        "optimize-autoloader": true,
        "process-timeout": 0
    }
}